class MongoOperators:
    addFields = "$addFields"
    addToSet = "$addToSet"
    and_ = "$and"
    as_ = "as"
    equal = "$eq"
    exists = "$exists"
    expr = "$expr"
    first = "$first"
    from_ = "from"
    greater = "$gt"
    greaterEqual = "$gte"
    group = "$group"
    in_ = "$in"
    input = "input"
    indexOfArray = "$indexOfArray"
    last = "$last"
    let = "let"
    limit = "$limit"
    literal = "$literal"
    lookup = "$lookup"
    lowerEqual = "$lte"
    map = "$map"
    match = "$match"
    max = "$max"
    min = "$min"
    newRoot = "newRoot"
    notEqual = "$ne"
    notIn = "$nin"
    options = "$options"
    or_ = "$or"
    pipeline = "pipeline"
    project = "$project"
    push = "$push"
    regex = "$regex"
    replaceRoot = "$replaceRoot"
    reverseArray = "$reverseArray"
    set = "$set"
    skip = "$skip"
    sort = "$sort"
    sum = "$sum"
    unwind = "$unwind"
